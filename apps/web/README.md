# apps/web

Next.js frontend (App Router) with role-based UI shell.

## Run
- `npm run dev --workspace @ehms/web`

## Current state
- Role-based login scaffold
- All module routes registered in UI navigation
- Leads first slice implemented: all, mine, details edit, soft delete, demo request

Next: wire Supabase auth session flow and module-by-module API integration.
